# wrangler.toml — Cloudflare Pages (FRONTEND ONLY)
# File ini hanya untuk deploy frontend statis ke Cloudflare Pages
# Backend (API + Auth + DB) sudah di Vercel

name = "jadwalkegiatan"
compatibility_date = "2026-07-22"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"
